consolidated.py -takes 3 parameters 

1) cpu raw results
2) gpu raw results
3) 0 or 1 
( 0 is for minimum while 1 is for median)
It generates two csv files one for the cpu and the other for the gpu

mergecsv.py - takes the csv files created previously and generates a consolidated file called mergednetwork


splitcsv.py - takes a parameter which is where to save data.( save it in ../data-generated/..rest of the path) . It reads the mergednetwork file and splits each benchmark seperately.
