data-scripts generates the csv files from the raw data , the master-script inside data-scripts folder takes three parameters the first is the cpu raw result and the second is the gpu raw result. The third is the path to where to save the generated csv , This is the data generation folder which has a similar directory structure to the raw result directory but now contains the consolidated csv's